import 'package:flutter/material.dart';

class HomeStartScreen extends StatelessWidget {
  const HomeStartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Home'));
  }
}
